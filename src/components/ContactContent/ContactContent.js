import ContactCard from "../ContactCard/ContactCard";
const ContactContent = ({contactArray, contactResults}) => {
  return (
      <div className="content">
      </div>
  );
};
export default ContactContent;
